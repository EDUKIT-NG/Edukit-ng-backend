const schoolProfileSchema = new Schema({
  schoolName: String,
  address: String,
  email: String,
  students: Number,
  address: String,
  contact: String,
  document: String,
});
// save updated info
// request resource
// save quantities, resource type
